version: "3.7"

services:
  bootstrap:
    build:
      context: ./bootstrap
      dockerfile: Dockerfile
      args:
        ZITI_VERSION: "1.1.15"
        SPIRE_VERSION: "1.10.3"
    image: openziti/ziti-bootstrap:latest
