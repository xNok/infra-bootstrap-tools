#################################################
# OR INFRA Role: Docker Swarm Node
# Source:
#    https://github.com/arillso/ansible.traefik
#    https://geek-cookbook.funkypenguin.co.nz/ha-docker-swarm/traefik/
#################################################

###
# GENERAL Setup
###
- name: Get first manager host vars
  set_fact:
    docker_swarm_node_first_manager: >-
      {{ hostvars[groups[docker_swarm_node_manager_inventory_group_name][0]] }}

- name: Get join-token for worker nodes
  set_fact:
    docker_swarm_node_join_token_worker: >-
      {{ docker_swarm_node_first_manager.docker_swarm_manager_result.swarm_facts.JoinTokens.Worker }}

###
# Add Nodes
###
- name: Add nodes
  docker_swarm:
    state: join
    advertise_addr: "{{ ansible_host }}"
    join_token: "{{ docker_swarm_node_join_token_worker }}"
    remote_addrs: "{{ groups[docker_swarm_node_manager_inventory_group_name] | map('extract', hostvars, ['ansible_host']) | join(',') }}"
