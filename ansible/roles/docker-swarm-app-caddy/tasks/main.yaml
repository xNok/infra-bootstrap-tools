#################################################
# OR INFRA Role: Caddy
# Source: https://caddyserver.com/docs/running#docker-compose
# Auth Portal: https://authp.github.io/docs/authenticate/auth-portal
#################################################
---
###
# GENERAL Setup
###
- name: Creates directory caddy
  file:
    path: "{{ caddy_dir }}"
    state: directory
    mode: '0644'

- name: Copy Compose file to remote server
  template:
    src: "{{ item }}"
    dest: "{{ caddy_dir }}"
    mode: '0644'
  with_fileglob:
    - "{{ caddy_assets_path }}/*-stack.yml"
    - "{{ caddy_assets_path }}/Caddyfile"
    - "{{ caddy_assets_path }}/Dockerfile"

###
# Create Caddy Pre-requisits
###
- name: Manager Caddy Configs
  include_role:
    name: utils-rotate-docker-configs
  vars:
    docker_compose_path: "{{ caddy_dir }}/caddy-stack.yml"
    configs:
      - {name: 'caddy_global_caddyfile', file_path: "{{ caddy_dir }}/Caddyfile"}

- name: Manager Caddy Secrets
  include_role:
    name: utils-rotate-docker-secrets
  vars:
    docker_compose_path: "{{ caddy_dir }}/caddy-stack.yml"
    secrets:
      - {name: 'caddy_github_client_id', value: "{{ caddy_github_client_id }}"}
      - {name: 'caddy_github_client_secret', value: "{{ caddy_github_client_secret }}"}
      - {name: 'caddy_jwt_shared_key', value: "{{ caddy_jwt_shared_key }}"}
      - {name: 'caddy_digitalocean_api_token', value: "{{ caddy_digitalocean_api_token }}"}

###
# Start Container
###
- name: Start caddy Stack
  docker_stack:
    state: present
    name: caddy
    compose:
      - "{{ caddy_dir }}/caddy-stack.yml"
